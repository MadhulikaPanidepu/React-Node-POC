runtime: nodejs20  
automatic_scaling:
  min_num_instances: 1
  max_num_instances: 2
handlers:
- url: /(.*\..+)$
  static_files: build/\1
  upload: build/(.*\..+)$
- url: /.*
  static_files: build/index.html
  upload: build/index.html
